version: '3.8'
services:
  app:
    container_name: backend-container
    build:
      context: ./backend
      target: development
    ports:
      - "8000:8000"
      - "8080:8080"
    environment:
      - SERVER_PORT=8080
    volumes:
      - ./backend:/app
